{
  "name": "javascript",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build --config vite.config.main.js && vite build --config vite.config.offscreen.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.1.1"
  },
  "dependencies": {
    "@mediapipe/tasks-vision": "^0.10.35"
  }
}